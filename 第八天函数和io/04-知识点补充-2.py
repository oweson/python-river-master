# a = 100
a = [100]


def test(num):
    # 1. 先算 =号右边的
    # 2. 算出来的结果是[100,100]
    # 3. num = [100,100]
    # 4. 让num变量指向了[100,100],即num保存的是[100,100]的引用(内存地址)
    num = num + num
    # num=num+num ========>[100]  + [100]====>[100,100]
    print(num)


test(a)

print(a)
